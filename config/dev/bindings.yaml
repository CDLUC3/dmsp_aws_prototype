template:
  path: bindings.yaml
  type: file

parameters:
  AlbArn: !stack_output dev/alb.yaml::AlbArn
  AlbTargetGroupArn: !stack_output dev/alb.yaml::AlbTargetGroupArn

  CertificateArn: !stack_output dev/cert.yaml::AlbCertificateArn

  WafArn: !stack_output dev/waf.yaml::WafArn
